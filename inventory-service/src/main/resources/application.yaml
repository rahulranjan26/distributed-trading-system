spring:
  application:
    name: inventory-service

  datasource:
    password: rahul@1995
    username: postgres
    url: jdbc:postgresql://localhost:5432/trading_inventory_db
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect


server:
  port: 8082

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true

